import React from "react";
import Header from "src/Components/Header";

type Props = {};

const Layout = ({}) => {
  return (
    <body className="w-[100%] h-[100svh] bg-white">
      <Header />
    </body>
  );
};

export default Layout;
